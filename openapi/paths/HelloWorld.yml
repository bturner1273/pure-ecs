get:
  tags:
    - HelloWorld
  operationId: SayHello
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: string
    "500":
      description: Internal Server Error
    "default":
      description: Application Error
      content:
        application/json:
          schema:
            $ref: "../responses/ApplicationError.yml"
